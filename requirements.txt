httpx>=0.27
selectolax>=0.3.21
python-dotenv>=1.0
pydantic>=2.0
PyYAML>=6.0
fpdf2>=2.7

# read-only web dashboard (applyfirst.web)
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1

# SaaS (applyfirst.saas) — envelope encryption + id_token signature verification
cryptography>=43          # AES-256-GCM at-rest encryption (abi3 wheel; runs on py3.14)
pyjwt>=2.8                # RS256 id_token verification against Google's JWKS
python-multipart>=0.0.9   # onboarding form parsing (FastAPI Form)
